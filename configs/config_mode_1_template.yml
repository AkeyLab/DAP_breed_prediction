# Paths
result_folder_path: './results/mode_1'
# One standardized toy dog; replace this with a compatible unlabeled genotype CSV.
SNP_csv_path: './data/Toy_X_single.csv'
# Raw score above this value is pure; otherwise the top two breeds are reported.
pure_threshold: 0.7
